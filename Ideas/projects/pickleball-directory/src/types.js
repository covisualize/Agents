export const ListingType = Object.freeze({
  VENUE: "venue",
  CLUB: "club",
  COACH: "coach"
});

export const SubmissionStatus = Object.freeze({
  PENDING: "pending",
  APPROVED: "approved",
  REJECTED: "rejected"
});

export const SponsoredTier = Object.freeze({
  BASIC: "basic",
  PLUS: "plus"
});
